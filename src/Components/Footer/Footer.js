import React from "react";
const prefix = "footer";
const Footer = () => {
  return (
    <footer className={prefix}>
      <h3 className={`${prefix}__h3`}>footer Imgage</h3>
    </footer>
  );
};

export default Footer;  
